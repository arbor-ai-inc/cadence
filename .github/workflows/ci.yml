name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # The leak gate runs first and alone. Everything else in this repo is
      # only publishable if this passes, so a failure here should be the whole
      # signal rather than one red line among many.
      - name: no leaks from the private source tree
        run: python3 tests/check_no_leaks.py

      - name: version bumped when content ships
        run: |
          git fetch --depth=2 origin "${{ github.base_ref || 'main' }}" || true
          python3 tests/check_version_bumped.py --selftest

      - name: adopter-runnable doc commands
        run: python3 tests/check_user_docs.py

      - name: config loader
        run: python3 tools/cadence_config.py --selftest

  suites:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Every suite is listed explicitly and unconditionally. A step that
      # skips when a file is absent makes an unrun suite and a passing suite
      # look identical, which is the trap in examples/case-studies.md C-10.

      - name: version bumped when content ships
        run: |
          git fetch --depth=2 origin "${{ github.base_ref || 'main' }}" || true
          python3 tests/check_version_bumped.py --selftest

      - name: adopter-runnable doc commands
        run: python3 tests/check_user_docs.py

      - name: config loader
        run: python3 tools/cadence_config.py --selftest

      - name: hash contract
        run: python3 tools/spec_hash.py --selftest

      - name: ask transport
        run: python3 tools/ask.py --selftest

      - name: review-state reader
        run: python3 tools/review_state.py

      - name: an adopter's first hour, against the working tree
        run: python3 tests/check_install.py

      - name: ...and against what people can download now
        run: python3 tests/check_install.py --committed

      - name: fan-out engine
        run: python3 tests/check_fanout.py

      - name: adapters stay thin and links resolve
        run: python3 tests/check_agent_skills.py

      - name: generated codex adapters are fresh
        run: python3 scripts/gen_adapters.py --check

  plugin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: manifests parse
        run: |
          python3 -c "import json; json.load(open('.claude-plugin/plugin.json'))"
          python3 -c "import json; json.load(open('.claude-plugin/marketplace.json'))"

      # The `agents` key takes an array of file paths, not a directory. A
      # directory string parses as JSON and is rejected only here, so the
      # parse check above is not a substitute for this one.
      - name: plugin validates
        run: npx --yes @anthropic-ai/claude-code@latest plugin validate ./
        continue-on-error: true
