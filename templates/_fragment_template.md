---
issue: XX-000           # issue id, or the PR number if there is no issue
pr: 000
traps:                  # one or more; reuse slugs from TRAPS.md, invent only if none fit
  - <slug>
cost: high              # high | medium | low — friction actually paid, not feared
candidate-home: <file>  # a guess at the destination doc; not binding on synthesis
---

What happened, in two to four sentences.

Observation only. No rule, no "always" or "never", no imperative. A fragment
records what the run hit and what it cost; whether that becomes a rule is decided
later, from how often the same trap appears across many fragments.

Cite the evidence: a PR review thread, a commit sha, an issue id, a command that
failed. A fragment whose claim cannot be checked against something is not worth
writing.
